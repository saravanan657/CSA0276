#include<stdio.h>
int main()
{
  char s[50]="Harshith",temp;
  int i=0,j;
  while(s[i]!='\0')
  {
    i++;
    j=i-1;
  }
  for(i=0;i<j;i++,j--)
  {
    temp=s[i];
    s[i]=s[j];
    s[j]=temp;
  }
  printf("Reversed string :%s",s);
  return 0;
}
